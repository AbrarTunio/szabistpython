# Lecture 2: Expressions

name = "Sadaf"
age  = 16

output = name + " is " +  str(age) + " years old."

# Plus is used for concatination if both values are string
# otherwise it will sum up values

print( output )